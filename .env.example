# Copy to .env.local for local overrides. The dev-runner derives ports
# deterministically per-checkout, so you usually don't need to set anything.

# Force a specific backend server port (otherwise auto-selected):
# APP_SERVER_PORT=13773

# Force a specific web dev-server port (otherwise auto-selected):
# APP_WEB_PORT=5733
